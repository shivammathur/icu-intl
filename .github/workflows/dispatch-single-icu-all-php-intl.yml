name: 'dispatch-single-icu-all-php-intl'
on:
  workflow_dispatch:
    inputs:
      icu:
        description: ICU version
        required: true
      rebuild:
        description: rebuild
        required: false

jobs:
  php-intl:
    container: ${{ matrix.container }}
    runs-on: ${{ matrix.os }}
    env:
      SKIP_PHP85: ${{ matrix.php == '8.5' && fromJSON(format('[{0}]', github.event.inputs.icu))[0] < 57 }}
    strategy:
      fail-fast: false
      matrix:
        php: ['5.6', '7.0', '7.1', '7.2', '7.3', '7.4', '8.0', '8.1', '8.2', '8.3', '8.4', '8.5']
        ts: ['zts', 'nts']
        os: [ubuntu-latest, ubuntu-22.04-arm]
        include:
          - os: ubuntu-latest
            container: ubuntu:jammy
          - os: ubuntu-22.04-arm
            container: arm64v8/ubuntu:jammy
    name: php-intl
    steps:
      - name: Skip unsupported PHP 8.5 build
        if: ${{ env.SKIP_PHP85 == 'true' }}
        run: echo "Skipping PHP 8.5 for ICU ${{ github.event.inputs.icu }}"
      - name: ${{github.event.inputs.php}}
        id: print_details
        if: ${{ env.SKIP_PHP85 != 'true' }}
        run: |
          echo sender=${{github.event.sender.login}}
          echo ICU=${{github.event.inputs.icu}}
      - name: Checkout
        uses: actions/checkout@v4
        if: ${{ env.SKIP_PHP85 != 'true' }}
      - name: Install requirements
        run: bash src/install-requirements.sh
        if: ${{ env.SKIP_PHP85 != 'true' }}
        env:
          TS: ${{ matrix.ts }}
          VERSION: ${{ matrix.php }}
      - name: Checkout
        uses: actions/checkout@v4
        if: ${{ env.SKIP_PHP85 != 'true' }}
      - name: Update intl
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ICU: ${{ github.event.inputs.icu }}
          REPO: ${{ github.repository }}
          TS: ${{ matrix.ts }}
          VERSION: ${{ matrix.php }}
          WORKSPACE: ${{ github.workspace }}
        if: ${{ env.SKIP_PHP85 != 'true' }}
        run: |
          existing_version=$(gh release view intl-$ICU 2>/dev/null | grep -Po "$VERSION-$ICU" | head -n 1)
          if [ "$VERSION-$ICU" != "$existing_version" ] || [ "x${{github.event.inputs.rebuild}}" = "xtrue" ]; then
            bash src/build-intl.sh
            bash src/release-intl.sh
          else
            echo "intl $VERSION-$ICU build exists"
          fi
